import { DashboardPlaceholder } from "@/components/dashboard/DashboardPlaceholder";

export default function SettingsPage() {
  return (
    <DashboardPlaceholder
      eyebrow="Settings"
      title="Workspace preferences will land here."
      description="Profile details, export defaults, and personalization controls can be layered into this page later. For now, it keeps the signed-in navigation coherent."
    />
  );
}
